Place saved Nav2 map files here.

The cleaning bringup defaults to:

```text
EERC_SB_LOOP.yaml
```

That YAML file should reference its matching image file, usually a `.pgm` or `.png`,
using the normal ROS map_server format.
