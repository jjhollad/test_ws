# Arduino backup

Synced from `~/Arduino` (sketches and custom libraries).

Arduino IDE board cores and bundled packages under `~/.arduino15` are **not** included — reinstall those on a new machine via the Board Manager.

Restore:

```bash
cp -a arduino ~/Arduino
```
